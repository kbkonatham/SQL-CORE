 Is used to join a table to a table-valued function.
 in the form of corelated sub query we get error .
 

CROSS APPLY = inner join.
OUTER APPLY= left outer join.

Like all table operators, this operator is used
in the FROM clause of a query. 
The two supported types of APPLY operator are CROSS APPLY and
OUTER APPLY. 

CROSS APPLY implements only one logical query processing phase,

whereas OUTER APPLY implements two. 
